using  BootcampDIO.Models;


    namespace  calculadora
    {
        class  CalculadoraProgram
        {
            static  void  Main(string[] args)
            {
                // var  calculadora  =  new  Calculadora();
                // calculadora.Somar(10, 5);
                // calculadora.Subtrair(10, 5);
                // calculadora.Multiplicar(10, 5);
                // calculadora.Dividir(10, 5);
                // calculadora.Potencia(10, 5);
                // calculadora.Seno(10);
                // calculadora.Cosseno(10);
                // calculadora.Tangente(10);   
                //calculadora.RaizQuadrada(10);

                // int numero = 10;
                // numero--; // ou ++ para incrementar
                // Console.WriteLine(numero);

            }
        }
    }